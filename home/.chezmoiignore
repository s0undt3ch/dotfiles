{{/* macOS-only targets: never apply them off macOS. */}}
{{ if ne .chezmoi.os "darwin" }}
Library
.config/metapac/groups/brew.toml
{{ end }}
{{/* Linux-only targets: never apply them on macOS. */}}
{{ if ne .chezmoi.os "linux" }}
.config/metapac/groups/arch.toml
{{ end }}
{{/* Work-only targets: skip on personal machines (see .chezmoi.toml.tmpl). */}}
{{ if not .work }}
.config/aerospace
.config/aerospace/**
.config/k9s
.config/k9s/**
.local/bin/claude-work
.local/bin/claude-personal
{{ end }}
{{/* nvim owns these at runtime; don't let chezmoi manage or fight them. */}}
.config/nvim/lazy-lock.json
.config/nvim/lazyvim.json
.DS_Store
