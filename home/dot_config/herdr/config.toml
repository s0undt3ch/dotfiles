# herdr configuration
# https://herdr.dev

onboarding = false

[keys]
  # Use CTRL-A as the prefix instead of the default CTRL-B.
  prefix = "ctrl+a"

[terminal]
  # New panes/tabs/workspaces (e.g. a split) inherit the source pane's
  # directory instead of defaulting to $HOME.
  new_cwd = "follow"

[theme]
  name        = "gruvbox"
  auto_switch = true
  dark_name   = "gruvbox"
  # No confirmed light "gruvbox" variant in herdr's builtin theme list, so both
  # modes point at the same dark-oriented theme for now.
  light_name = "gruvbox"

[ui]
  # Without this, a single-pane workspace loses the outer frame that
  # otherwise separates it from the sidebar.
  pane_outer_borders = true

  [ui.toast]
    delivery = "system"
