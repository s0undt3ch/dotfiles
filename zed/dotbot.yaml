- link:
    ~/.config/zed/settings.json: settings.json

- if:
    cond: brew ls --versions --cask zed &>/dev/null
    unmet:
      - shell:
          - command: brew install --cask zed
            stdin: true
            stdout: true
            stderr: true
