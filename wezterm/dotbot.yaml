- link:
    ~/.config/wezterm/wezterm.lua: config.lua

- if:
    cond: brew ls --versions wezterm
    unmet:
    - shell:
      - command: brew install wezterm
        stdin: true
        stdout: true
        stderr: true
