- if:
    cond: brew ls --versions --cask font-sauce-code-pro-nerd-font &>/dev/null
    unmet:
    - shell:
      - command: brew install --cask font-sauce-code-pro-nerd-font
        stdin: true
        stdout: true
        stderr: true

- if:
    cond: brew ls --versions --cask font-proggy-clean-tt-nerd-font &>/dev/null
    unmet:
    - shell:
      - command: brew install --cask font-proggy-clean-tt-nerd-font
        stdin: true
        stdout: true
        stderr: true

- if:
    cond: brew ls --versions --cask font-hack-nerd-font &>/dev/null
    unmet:
    - shell:
      - command: brew install --cask font-hack-nerd-font
        stdin: true
        stdout: true
        stderr: true
