- if:
    cond: brew ls --versions --cask ghostty &>/dev/null
    unmet:
      - shell:
          - command: brew install --cask ghostty
            stdin: true
            stdout: true
            stderr: true

- link:
    ~/.config/ghostty/config: ghostty.conf
