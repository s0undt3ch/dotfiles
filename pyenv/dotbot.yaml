- if:
    cond: brew ls --versions sqlite3 &>/dev/null
    unmet:
    - shell:
      - command: brew install sqlite3
        stdin: true
        stdout: true
        stderr: true

- if:
    cond: brew ls --versions tcl-tk &>/dev/null
    unmet:
    - shell:
      - command: brew install tcl-tk
        stdin: true
        stdout: true
        stderr: true

- if:
    cond: brew ls --versions pyenv &>/dev/null
    unmet:
      - shell:
          - command: brew install pyenv
            stdin: true
            stdout: true
            stderr: true

- if:
    cond: brew ls --versions pyenv-virtualenv &>/dev/null
    unmet:
      - shell:
          - command: brew install pyenv-virtualenv
            stdin: true
            stdout: true
            stderr: true
