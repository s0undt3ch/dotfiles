- link:
    ~/.config/starship.toml: config.toml

- if:
    cond: brew ls --versions starship &>/dev/null
    unmet:
    - shell:
      - command: brew install starship
        stdin: true
        stdout: true
        stderr: true
