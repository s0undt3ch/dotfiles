- if:
    cond: brew ls --versions --cask alt-tab &>/dev/null
    unmet:
      - shell:
          - command: brew install --cask alt-tab
            stdin: true
            stdout: true
            stderr: true
