- link:
    ~/.config/mise/config.toml: mise.toml

- if:
    cond: brew ls --versions mise &>/dev/null
    unmet:
    - shell:
      - command: brew install mise
        stdin: true
        stdout: true
        stderr: true
