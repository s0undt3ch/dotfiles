- defaults:
    link:
      create: true
      relink: true
      glob: true

- clean:
  - '~'

- includes:
    zsh: dotbot.yaml
    kitty: dotbot.yaml

- includes:
    tmux: dotbot.yaml
    firefox: dotbot.yaml

- includes:
    pacdef: dotbot.yaml
