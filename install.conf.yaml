- defaults:
    link:
      create: true
      relink: true
      glob: true

- clean:
  - '~'

- includes:
    zsh: dotbot.yaml
    kitty: dotbot.yaml
    neovim: dotbot.yaml
    tmux: dotbot.yaml
    firefox: dotbot.yaml

- includes:
    pacdef: dotbot.yaml
