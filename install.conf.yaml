- defaults:
    link:
      create: true
      relink: true
      glob: true

- clean:
  - '~'

- includes:
    pyenv: dotbot.yaml
    zsh: dotbot.yaml
    neovim: dotbot.yaml
    tmux: dotbot.yaml
#    firefox: dotbot.yaml
    wezterm: dotbot.yaml
    starship: dotbot.yaml
    keyboard: dotbot.yaml

#- includes:
#    pacdef: dotbot.yaml
