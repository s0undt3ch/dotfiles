- if:
    cond: brew ls --versions naps2 &>/dev/null
    unmet:
      - shell:
          - command: brew install --cask naps2
            stdin: true
            stdout: true
            stderr: true
